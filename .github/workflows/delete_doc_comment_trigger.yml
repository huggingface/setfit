name: Delete doc comment trigger

on:
  pull_request:
    types: [ closed ]


jobs:
  delete:
    uses: huggingface/doc-builder/.github/workflows/delete_doc_comment_trigger.yml@9f9bb430a712b5650d0e43e510656098ecdd75d4  # main
    with:
      pr_number: ${{ github.event.number }}